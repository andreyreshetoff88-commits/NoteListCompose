package ru.reshetoff.common.ui

import androidx.compose.ui.unit.sp

object TextSize {
    val small = 12.sp    // мелкий текст (подсказки, ошибки)
    val medium = 16.sp   // обычный текст (основной)
    val large = 24.sp    // крупный текст (заголовки)
}